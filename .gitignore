.superpowers/
node_modules/
dist/
out/
build/*
# Keep the electron-builder entitlements file even though build/ is ignored.
!build/entitlements.mac.plist
# Keep the electron-builder afterPack hook even though build/ is ignored.
!build/trim-electron-framework.cjs
# Keep the macOS app icon (+ its regeneration source) even though build/ is ignored.
!build/icon.icns
!build/icon-1024.png
.DS_Store
*.log
.env
.env.local
__pycache__/
*.pyc
.venv/
venv/
sidecar/.venv*/

# Build artifacts
release/
sidecar/dist/
sidecar/build/
sidecar/*.spec
sidecar/*.egg-info/

# Worktrees / local tooling
.claude/
.worktrees/

# Phase 0 research scratch — not committed
research/
